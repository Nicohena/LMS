NODE_ENV=
PORT=
DATABASE_URL=

# JWT
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# CORS
CLIENT_URL=http://localhost:3000

# Cloudinary (file uploads)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Redis (for BullMQ + caching) — optional, falls back to in-memory if not set
REDIS_HOST=
REDIS_PORT=
REDIS_PASSWORD=

# Email (for notifications — Step 8)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# Plagiarism Detection (optional — Step 8)
TURNITIN_API_KEY=
COPYLEAKS_API_KEY=

# Socket.io (Step 9) — defaults to the HTTP port if not set
SOCKET_PORT=
